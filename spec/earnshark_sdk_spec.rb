require "spec_helper"

describe EarnsharkSdk do
  it "has a version number" do
    expect(EarnsharkSdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
